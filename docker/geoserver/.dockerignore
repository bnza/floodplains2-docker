# Ignore volatile runtime data inside the data directory
data/logs/
data/temp/
data/tmp/

# Ignore all subdirectories in GWC (this catches the tile folders)
# We want to keep the config files in the gwc/ root
data/gwc/**
!data/gwc/*.xml

# Specifically ignore log files if they are in the root of data/
data/*.log
